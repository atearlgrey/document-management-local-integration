version: '3.8'
services:
  postgres:
    image: postgres:14
    container_name: keycloak_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      TZ: Asia/BangKok           # ✅ Đặt timezone hệ thống container
      PGTZ: Asia/BangKok         # ✅ Đặt timezone cho PostgreSQL
    ports:
      - "5432:5432"
    volumes:
      # Moved DB initialization to Flyway migrations. Keep persistent data volume only.
      - ./postgres/init:/docker-entrypoint-initdb.d
      - postgres_data:/var/lib/postgresql/data
    networks:
      - document-management-net

  keycloak:
    image: atearlgrey/plant-growth-identity:latest   # phiên bản ổn định
    container_name: keycloak_server
    restart: always
    environment:
      - KEYCLOAK_ADMIN=admin
      - KEYCLOAK_ADMIN_PASSWORD=admin
      - KC_DB=postgres
      - KC_DB_URL_HOST=postgres
      - KC_DB_URL_DATABASE=keycloak
      - KC_DB_USERNAME=keycloak
      - KC_DB_PASSWORD=keycloak
      - KC_HOSTNAME=localhost
    command:
      - start-dev
      - --import-realm
    ports:
      - "8080:8080"
    depends_on:
      - postgres
    volumes:
      - ./keycloak-init/realm/:/opt/keycloak/data/import/
    networks:
      - document-management-net
  
  keycloak-importer:
    image: alpine:3.20
    container_name: keycloak_importer
    environment:
      - KEYCLOAK_ADMIN=admin
      - KEYCLOAK_ADMIN_PASSWORD=admin
      - REALM=plant-growth
      - KEYCLOAK_URL=http://keycloak:8080
    depends_on:
      - keycloak
    volumes:
      - ./keycloak-init/:/opt/keycloak/data/import/
    entrypoint: >
      /bin/sh -c "
        apk add --no-cache curl jq;
        chmod +x /opt/keycloak/data/import/import-keycloak-api.sh;
        /opt/keycloak/data/import/import-keycloak-api.sh
      "
    networks:
      - document-management-net

  flyway:
    image: flyway/flyway:9
    container_name: flyway
    entrypoint: [ "bash" ]
    environment:
      - FLYWAY_CONNECT_RETRIES=10
      - FLYWAY_BASELINE_VERSION=0
    command: >
      -c "
        /flyway/flyway -configFiles=/flyway/keycloak/conf/flyway-ddl.conf -user=keycloak -password=keycloak -baselineOnMigrate="true" migrate;
        /flyway/flyway -configFiles=/flyway/keycloak/conf/flyway-master-data.conf -user=keycloak -password=keycloak -baselineOnMigrate="true" migrate;
        /flyway/flyway -configFiles=/flyway/keycloak/conf/flyway-test-data.conf -user=keycloak -password=keycloak -baselineOnMigrate="true" migrate;
      "
    volumes:
      - ./flyway-migration-files/keycloak:/flyway/keycloak:ro
    depends_on:
      - postgres
      - keycloak
    networks:
      - document-management-net

  document-app:
    image: atearlgrey/document-management-app-demo
    container_name: document-app
    restart: always
    environment:
      - PRODUCTION="true"
      - ENVIRONMENT="production"
      - ENABLE_AUTH="true"
      - AUTHORITY_URL=http://localhost:8080
      - AUTHORITY_REALM=plant-growth
      - AUTHORITY_CLIENT_ID=plant-growth-client
      - SERVICE_URL=http://localhost:4301
    depends_on:
      - keycloak
    networks:
      - document-management-net
  
volumes:
  postgres_data:

networks:
  document-management-net:
    driver: bridge
